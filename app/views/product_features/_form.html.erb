<%= form_with(model: product_feature, local: true) do |form| %>
  <% if product_feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_feature.errors.count, "error") %> prohibited this product_feature from being saved:</h2>

      <ul>
        <% product_feature.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.number_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :feature_id %>
    <%= form.number_field :feature_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
